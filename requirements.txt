# Companion code for "Build an Advanced RAG Application (From Scratch)" — Hamza Farooq, Manning
# Single environment covering the whole book (chapter 2's listings through chapter 9).
# Setup:
#   conda create -n advanced-rag python=3.11 -y
#   conda activate advanced-rag
#   pip install -r requirements.txt

# --- Core scientific stack ---
numpy>=1.26
pandas>=2.2
scipy>=1.13

# --- Notebook runtime ---
jupyter>=1.0
ipykernel>=6.29

# --- NLP utilities ---
nltk>=3.8

# --- Embedding models ---
# transformers is pinned below 5: nomic-embed-text-v1.5 ships custom model code that
# calls get_extended_attention_mask, which Transformers 5 removed (AttributeError on
# model.encode). Verified working on transformers 4.57 + sentence-transformers 5.5.
sentence-transformers>=3.0,<6
transformers>=4.41,<5
torch>=2.2
einops>=0.8

# --- HuggingFace data ---
datasets>=2.19
huggingface-hub>=0.23

# --- Vector search ---
faiss-cpu>=1.8
qdrant-client>=1.9

# --- LLM clients ---
openai>=1.30

# --- Chapter 6b extras ---
kagglehub>=0.2
plotly>=5.22
matplotlib>=3.9
rich>=13.7
gradio>=4.31

# --- Chapter 7 extras ---
pymupdf>=1.24       # PDF text extraction for 10-Ks and OpenAI agents guide
nest-asyncio>=1.6   # await inside Jupyter
requests>=2.32      # SerpAPI web search

# --- Local secrets (replaces google.colab.userdata) ---
python-dotenv>=1.0
